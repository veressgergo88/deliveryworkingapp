import React from 'react'
import ReactDOM from 'react-dom/client'
import Deldaywork from './components/Deldaywork'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Deldaywork />
  </React.StrictMode>,
)
